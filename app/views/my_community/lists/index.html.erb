<h1>Ma communauté</h1>

<ul>
 <% @lists.each do |list| %>
  <li>
    <%= "#{list.owner.full_name}" %>
    <%= link_to my_community_list_path(list) do %>
      <i class="fa fa-user"></i>
    <% end %>
  </li>
 <% end %>
</ul>
